# Changelog

## 0.2.1 (2026-08-14)

独立评测（稳定/可控/可分发三维）后的 P1 修复轮，无 P0：

- P1-1 财报门本地二次过滤事件日期——CLI 的 --end 实测会放行窗口外事件，曾致 NOK 被误拒且 ⚠ 文案印出错误事实（已实测 NOK exit 0 / NVDA 仍 exit 5）
- P1-2 渲染层补年化恒等式断言，篡改 ann 现被拒渲染（mutation test 由 fail 转 pass）
- P1-3 run.py 重跑时文案随数据强制重建，消除"新数据+旧文案"自相矛盾
- P2-4 PCR 为 null 时渲染显示 — 而非崩溃；P2-6 README 版本号漂移改为指向 CHANGELOG
- 新增 tests/：假 CLI（9 种环境模式）+ run_tests.sh 一键离线回归 15 项（教训：测试资产曾放系统临时目录被清空，评测时不可用，故固化入仓）

遗留 P2（评测报告存档待拍板）：脏数据裸 traceback 收敛、exit 6 误诊、BRK.B 带点代码、segments schema 校验（路径 B 体验关键）、拒跑后旧数据可渲染、IV≥300% 单位启发式、缺档静默。

## 0.2.0 (2026-08-14)

AI 运行时适配：不再假设用户有 Claude。

- 新增三级用法：A Claude Code 全自动 / B 任意 AI 助手（PROMPT.md 通用提示词）/ C 零 AI（run.py 一键，固定规则文案）
- 新增 make_segments.py：纯规则生成中性数据陈述文案（_source=auto）
- 新增 run.py 一键入口：doctor → 取数 → 自动文案 → 渲染 → 打开（--skip-doctor / --no-open / --allow-earnings）
- 新增 PROMPT.md：运行时无关的通用 AI 提示词（含占位符表与全部写作纪律）
- 责任分区：段落标签随 _source 切换——AI 起草挂「AI 视角」，规则生成挂「自动摘要」，口径说明同步改写，不让机械文案冒充 AI 观点

## 0.1.1 (2026-08-14)

沙盒净装 + 双盲用户测试后的修复轮：

- fix: 三脚本加 PEP 263 编码声明（macOS 系统 Python 3.9 tokenizer 对超长中文行误报 Non-UTF-8，净装沙盒捕获）
- SKILL.md: 补占位符符号对照表 + 12 条歧义裁定细则（术语白名单 / 墙厚度口径 / sym_cn 规则 / 单位换算边界等，均来自双盲 sub-agent 回报）
- 明示 build_report.py 从当前目录读取的输入契约；工作目录建在用户当前目录、日期统一 %F
- fetch 摘要输出补双墙 OI 数值（供起草"墙厚度"时直接引用）
- README: 手动 clone 使用说明

## 0.1.0 (2026-08-14)

首个可分发版本。自内部「期权卖方日报」管线（No.001–No.003 实战验证）轻量化重构：

- 自包含取数：砍掉 option-flow 插件依赖，IV/HV/PCR/Max Pain 全部窗口内自算，一趟取数
- 到期日自选：从真实到期日列表选最贴 30/37/44 天的三档（兼容周一/三/五周度与仅月度标的）
- 财报门代码化：finance-calendar 查询窗口内财报，命中即拒跑（exit 5）
- doctor 预检：CLI / 登录态 / 期权链 / 合约级行情权限四连检，人话修复指引
- 错误码契约：0/2/3/4/5/6，非零禁止渲染
- 渲染器：品牌双页 A4，轴刻度步长自适应（$3–$8000 标的均可画），页面直改 + 导出编辑版
